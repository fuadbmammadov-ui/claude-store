<nav class="navbar navbar-expand-lg navbar-dark bg-dark no-print">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Mağaza POS</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="nav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><a class="nav-link <%= path==='/' ? 'active' : '' %>" href="/"><i class="bi bi-speedometer2"></i> Panel</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/pos') ? 'active' : '' %>" href="/pos"><i class="bi bi-cart3"></i> Satış</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/products') ? 'active' : '' %>" href="/products"><i class="bi bi-box-seam"></i> Mallar</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/customers') ? 'active' : '' %>" href="/customers"><i class="bi bi-people"></i> Müştərilər / Borclar</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/cash-sessions') ? 'active' : '' %>" href="/cash-sessions"><i class="bi bi-cash-coin"></i> Kassa</a></li>
        <% if (currentUser && currentUser.role === 'ADMIN') { %>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/expenses') ? 'active' : '' %>" href="/expenses"><i class="bi bi-receipt"></i> Xərclər</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/suppliers') ? 'active' : '' %>" href="/suppliers"><i class="bi bi-truck"></i> Təchizatçılar</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/reports') ? 'active' : '' %>" href="/reports"><i class="bi bi-bar-chart-line"></i> Hesabatlar</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/users') ? 'active' : '' %>" href="/users"><i class="bi bi-person-gear"></i> İstifadəçilər</a></li>
        <% } else { %>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/expenses') ? 'active' : '' %>" href="/expenses/new"><i class="bi bi-receipt"></i> Xərc yaz</a></li>
        <% } %>
      </ul>
      <span class="navbar-text me-3 text-white-50">
        <%= currentUser ? currentUser.fullName + ' (' + (currentUser.role === 'ADMIN' ? 'Admin' : 'Satıcı') + ')' : '' %>
      </span>
      <form action="/logout" method="post" class="d-flex">
        <button class="btn btn-outline-light btn-sm" type="submit">Çıxış</button>
      </form>
    </div>
  </div>
</nav>
