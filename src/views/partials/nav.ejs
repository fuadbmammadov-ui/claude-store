<nav class="navbar navbar-expand-lg navbar-dark bg-dark no-print">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Mağaza POS</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#nav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="nav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item"><a class="nav-link <%= path==='/' ? 'active' : '' %>" href="/">Panel</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/pos') ? 'active' : '' %>" href="/pos">Satış</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/products') ? 'active' : '' %>" href="/products">Mallar</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/customers') ? 'active' : '' %>" href="/customers">Müştərilər / Borclar</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/cash-sessions') ? 'active' : '' %>" href="/cash-sessions">Kassa</a></li>
        <% if (currentUser && currentUser.role === 'ADMIN') { %>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/reports') ? 'active' : '' %>" href="/reports">Hesabatlar</a></li>
        <li class="nav-item"><a class="nav-link <%= path && path.startsWith('/users') ? 'active' : '' %>" href="/users">İstifadəçilər</a></li>
        <% } %>
      </ul>
      <span class="navbar-text me-3 text-white-50">
        <%= currentUser ? currentUser.fullName + ' (' + (currentUser.role === 'ADMIN' ? 'Admin' : 'Satıcı') + ')' : '' %>
      </span>
      <form action="/logout" method="post" class="d-flex">
        <button class="btn btn-outline-light btn-sm" type="submit">Çıxış</button>
      </form>
    </div>
  </div>
</nav>
